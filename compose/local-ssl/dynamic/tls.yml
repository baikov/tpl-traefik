# Add your local certificate here...
# cd this/folder
# mkcert -install
# mksert "*.domain.local"
tls:
  certificates:
    - certFile: /etc/certs/tpl.local.pem
      keyFile: /etc/certs/tpl.local-key.pem
